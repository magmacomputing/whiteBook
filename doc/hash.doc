/**
 * to provide a SHA 256 signature of an object
 */

snap(store, changes) {
prev = idb.get('_local_', store)
	
on Snap #0
	await idb.getAll()
	JSON.stringify()				(what about preserved order of key:values?)
	getHash()

	if (hash === prev)
		goto loadCache;				# nothing more to do
	await idb.clear(store)	# complete rebuild needed
end

changes.forEach()

await idb.getAll()
JSON.stringify()
getHash()
idb.put('_local_', store, hash)

loadCache:
this.cache[store] = 
}