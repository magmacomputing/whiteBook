
*> Plan UI... only show mat-card if not plan._hidden

*> spinner when initial-loading ?  or add 'plan' to customClaims so quicker?
so no false 'select plan'

*> account component
allow payment, hold

*> migrate User, connect to FaceBook
(works if Google first)
permissions error if Github

*> Profile detail
granted_scopes ?