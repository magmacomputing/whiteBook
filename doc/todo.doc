
*> add createDate / updateDate to State?
but remove on fire.write?
(skip:  takes too long on UI thread)

*> spinner when initial-loading ?  or add 'plan' to customClaims so quicker?
so no false 'select plan'

*> account component
allow payment, hold

*> migrate User
test if multiple instances of same source

*> logon as other User
set authState.user ?  or authState.delegate

*> logging to FireStore

*> add 'note' to /attend

*> block multiple check-in for same day (unless 'note') in /attend

*> SnackService
re-write AuthState to use