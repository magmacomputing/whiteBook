import { IProfileInfo } from '@dbase/data/data.schema';


// Library of member-related functions

interface IInfo {

}

// assemble a standard Member Info object
export const getProfileInfo = (list: IProfileInfo[]) => {
  const data:IInfo[]= [];

  list.forEach(info => {

  })

  return data;
}