This folder can be removed after the WhiteBook sheets have been migrated to Cloud Firestore

It is lazy-loaded, so it should not impact UX in the meantime.

It is included in this project, so that it can leverage the existing CheckIn processes.
